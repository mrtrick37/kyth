name: Build container image
on:
  schedule:
    - cron: '05 10 * * *'  # 10:05am UTC daily — rebuilds both :latest and :testing with fresh upstream packages
  push:
    branches:
      - main
      - testing
    paths-ignore:
      - '**/README.md'
  workflow_dispatch:

env:
  IMAGE_DESC: "Kyth — atomic gaming and dev workstation built on Fedora Kinoite"
  IMAGE_KEYWORDS: "bootc,ublue,universal-blue"
  IMAGE_LOGO_URL: "https://avatars.githubusercontent.com/u/120078124?s=200&v=4"  # Put your own image here for a fancy profile on https://artifacthub.io/!
  IMAGE_NAME: "${{ github.event.repository.name }}"  # output image name, usually same as repo name
  IMAGE_REGISTRY: "ghcr.io/${{ github.repository_owner }}"  # do not edit
  # Opt into Node.js 24 for all actions — required before June 2026 when Node 20 is dropped.
  # Remove this line once docker/login-action and docker/metadata-action pins are updated
  # to SHA-pinned releases that natively target Node.js 24.
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build_push:
    name: Build and push (${{ matrix.branch }})
    runs-on: ubuntu-24.04

    # One slot per branch — prevents a scheduled run and a push from racing on the same tag.
    concurrency:
      group: ${{ github.workflow }}-${{ matrix.branch }}
      cancel-in-progress: false

    strategy:
      fail-fast: false
      matrix:
        # push → build only the branch that was pushed (single-item list)
        # schedule / workflow_dispatch → build both branches in parallel
        branch: ${{ github.event_name == 'push' && fromJSON(format('["{0}"]', github.ref_name)) || fromJSON('["main","testing"]') }}

    permissions:
      contents: read
      packages: write
      id-token: write

    steps:
      - name: Prepare environment
        id: prep
        run: |
          echo "IMAGE_REGISTRY=${IMAGE_REGISTRY,,}" >> ${GITHUB_ENV}
          echo "IMAGE_NAME=${IMAGE_NAME,,}" >> ${GITHUB_ENV}
          # main → :latest, everything else → :<branch-name>
          TAG="${{ matrix.branch == 'main' && 'latest' || matrix.branch }}"
          echo "DEFAULT_TAG=${TAG}" >> ${GITHUB_ENV}
          echo "default_tag=${TAG}" >> ${GITHUB_OUTPUT}

      # These stage versions are pinned by https://github.com/renovatebot/renovate
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
        with:
          ref: ${{ matrix.branch }}

      - name: Capture checked-out commit
        id: checkout_sha
        run: echo "sha=$(git rev-parse HEAD)" >> $GITHUB_OUTPUT

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@b5ca514318bd6ebac0fb2aedd5d36ec1b5c232a2 # v3.10.0

      - name: Maximize build space
        uses: jlumbroso/free-disk-space@54081f138730dfa15788a46383842cd2f914a1be # v1.3.1
        with:
          android: true
          dotnet: true
          haskell: true
          large-packages: false
          docker-images: false
          swap-storage: true

      - name: Get current date
        id: date
        run: |
          # This generates a timestamp like what is defined on the ArtifactHub documentation
          # E.G: 2022-02-08T15:38:15Z'
          # https://artifacthub.io/docs/topics/repositories/container-images/
          # https://linux.die.net/man/1/date
          echo "date=$(date -u +%Y\-%m\-%d\T%H\:%M\:%S\Z)" >> $GITHUB_OUTPUT

      # Image metadata for https://artifacthub.io/ - This is optional but is highly recommended so we all can get a index of all the custom images
      # The metadata by itself is not going to do anything, you choose if you want your image to be on ArtifactHub or not.
      - name: Image Metadata
        uses: docker/metadata-action@c299e40c65443455700f0fdfc63efafe5b349051 # v5
        id: metadata
        with:
          # This generates all the tags for your image, you can add custom tags here too!
          # Default tags are "$DEFAULT_TAG" and "$DEFAULT_TAG.$date".
          tags: |
            type=raw,value=${{ steps.prep.outputs.default_tag }}
            type=raw,value=${{ steps.prep.outputs.default_tag }}.{{date 'YYYYMMDD'}}
            type=raw,value={{date 'YYYYMMDD'}},enable=${{ matrix.branch == 'main' }}
          labels: |
            io.artifacthub.package.readme-url=https://raw.githubusercontent.com/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}/${{ steps.checkout_sha.outputs.sha }}/README.md
            org.opencontainers.image.created=${{ steps.date.outputs.date }}
            org.opencontainers.image.description=${{ env.IMAGE_DESC }}
            org.opencontainers.image.documentation=https://raw.githubusercontent.com/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}/${{ steps.checkout_sha.outputs.sha }}/README.md
            org.opencontainers.image.source=https://github.com/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}/blob/${{ steps.checkout_sha.outputs.sha }}/Dockerfile
            org.opencontainers.image.title=${{ env.IMAGE_NAME }}
            org.opencontainers.image.url=https://github.com/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}/tree/${{ steps.checkout_sha.outputs.sha }}
            org.opencontainers.image.vendor=${{ github.repository_owner }}
            org.opencontainers.image.version=${{ steps.prep.outputs.default_tag }}.{{date 'YYYYMMDD'}}
            io.artifacthub.package.deprecated=false
            io.artifacthub.package.keywords=${{ env.IMAGE_KEYWORDS }}
            io.artifacthub.package.license=Apache-2.0
            io.artifacthub.package.logo-url=${{ env.IMAGE_LOGO_URL }}
            io.artifacthub.package.prerelease=${{ matrix.branch != 'main' }}
            containers.bootc=1
          sep-tags: " "
          sep-labels: "\n"
          sep-annotations: "\n"

      - name: Login to GitHub Container Registry
        uses: docker/login-action@74a5d142397b4f367a81961eba4e8cd7edddf772 # v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Resolve latest GE-Proton version
        id: ge_proton
        run: |
          VER=$(curl -fsSL \
            -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
            https://api.github.com/repos/GloriousEggroll/proton-ge-custom/releases/latest \
            | python3 -c "import sys,json; print(json.load(sys.stdin)['tag_name'])")
          echo "version=${VER}" >> $GITHUB_OUTPUT
          echo "Resolved GE-Proton: ${VER}"

      - name: Build base image
        run: |
          IMAGE_FULL="ghcr.io/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}"
          BASE_CACHE_REF="${IMAGE_FULL}:buildcache-base-${{ matrix.branch }}"
          BASE_IMAGE_REF="${IMAGE_FULL}:base-${{ steps.prep.outputs.default_tag }}"

          docker buildx build \
            --build-arg BASE_IMAGE=ghcr.io/ublue-os/kinoite-main:44 \
            --cache-from "type=registry,ref=${BASE_CACHE_REF}" \
            --cache-to "type=registry,ref=${BASE_CACHE_REF},mode=max" \
            --tag "${BASE_IMAGE_REF}" \
            --push \
            build_base/

      - name: Build final image
        run: |
          IMAGE_FULL="ghcr.io/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}"
          FINAL_CACHE_REF="${IMAGE_FULL}:buildcache-final-${{ matrix.branch }}"
          BASE_IMAGE_REF="${IMAGE_FULL}:base-${{ steps.prep.outputs.default_tag }}"

          TAG_ARGS=()
          for tag in ${{ steps.metadata.outputs.tags }}; do
            TAG_ARGS+=(--tag "${IMAGE_FULL}:${tag}")
          done

          docker buildx build \
            --secret id=github_token,env=GITHUB_TOKEN \
            --build-arg BASE_IMAGE="${BASE_IMAGE_REF}" \
            --build-arg GE_PROTON_VER="${{ steps.ge_proton.outputs.version }}" \
            --cache-from "type=registry,ref=${FINAL_CACHE_REF}" \
            --cache-to "type=registry,ref=${FINAL_CACHE_REF},mode=max" \
            "${TAG_ARGS[@]}" \
            --push \
            .
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Enable containerd snapshotter for runtime image inspection
        run: |
          # Avoid overlay2 lowerdir limits when pulling very deep images for docker run.
          sudo systemctl stop docker docker.socket 2>/dev/null || true
          echo '{"features":{"containerd-snapshotter":true}}' | sudo tee /etc/docker/daemon.json
          sudo systemctl start docker

          echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin

      # Save exact RPM versions from the built image for supply-chain diffing
      # between daily runs.
      - name: Capture RPM manifest
        run: |
          IMAGE_FULL="ghcr.io/${{ github.repository_owner }}/${{ env.IMAGE_NAME }}"
          OUT_FILE="rpm-manifest-${{ matrix.branch }}-${{ steps.prep.outputs.default_tag }}.txt"
          docker run --rm "${IMAGE_FULL}:${{ steps.prep.outputs.default_tag }}" \
            bash -lc "rpm -qa --qf '%{name}-%{version}-%{release}.%{arch}\\n' | sort" \
            > "${OUT_FILE}"

      - name: Upload RPM manifest artifact
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: rpm-manifest-${{ matrix.branch }}-${{ github.run_id }}
          path: rpm-manifest-${{ matrix.branch }}-${{ steps.prep.outputs.default_tag }}.txt

      - name: Install Cosign
        uses: sigstore/cosign-installer@faadad0cce49287aee09b3a48701e75088a2c6ad # v4.0.0
        with:
          cosign-release: 'v2.6.1'

      - name: Sign container image
        run: |
          IMAGE_FULL="${{ env.IMAGE_REGISTRY }}/${{ env.IMAGE_NAME }}"
          for tag in ${{ steps.metadata.outputs.tags }}; do
            cosign sign -y --key env://COSIGN_PRIVATE_KEY $IMAGE_FULL:$tag
          done
        env:
          COSIGN_EXPERIMENTAL: false
          COSIGN_PRIVATE_KEY: ${{ secrets.SIGNING_SECRET }}
